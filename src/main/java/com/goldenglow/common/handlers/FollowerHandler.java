package com.goldenglow.common.handlers;

public class FollowerHandler {
    /*public Map<EntityPlayer, ScriptNpc> followMap = new HashMap<EntityPlayer, ScriptNpc>();

    public void stepUpdate() {
        for(ScriptNpc npc : followMap.values()) {
            if(!npc.getTempData("lastX").equals(npc.getBlockX()) || !npc.getTempData("lastZ").equals(npc.getBlockZ())) {
                npc.setTempData("lastX", npc.getBlockX());
                npc.setTempData("lastZ", npc.getBlockZ());
                npc.setTempData("lastRand", Math.random());
            }
        }
    }*/
}
